//Random using bits
double next(int bits);

//Random Integer 32 bits
int next_int(void);

int next_int(int max);

int next_int(int min, int max);

//Random Integer 64 bits
long next_long(void);

//Random Integer with maximum value
long next_long(long max);

//Random Integer with minimum and maximum value
long next_long(long min, long max);

//Random Decimal 32 bits
float next_float(void);

float next_float(float max);

float next_float(float min, float max);

//Random Decimal 64 bits
double next_double(void);

double next_double(double max);

double next_double(double min, double max);

//Random bool with percent
bool chance(double percent);

//Random bool 50% true and false
bool next_bool();

